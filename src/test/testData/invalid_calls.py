<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type(obj = 2)</warning>

<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type(b'2', foo = 'bar')</warning>

<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type()</warning>


def f():
	<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type(
		obj = 2
	)</warning>

	<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type(
		b'2',
		foo = 'bar'
	)</warning>
	
	<warning descr="`reveal_type()` expects exactly one positional argument">reveal_type(
	)</warning>
